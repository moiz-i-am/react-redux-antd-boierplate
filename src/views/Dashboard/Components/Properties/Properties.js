import React from "react";
import { withRouter } from "react-router";

const Properties = (props) => (
  <div style={{ padding: 24, height: "100vh", minHeight: 500 }}>Properties</div>
);

export default withRouter(Properties);
